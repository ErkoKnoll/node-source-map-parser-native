{
  "name": "node-source-map-parser-native",
  "version": "0.1.0",
  "description": "Native source map parser for Node",
  "main": "lib/index.js",
  "author": "Erko Knoll",
  "license": "MIT",
  "dependencies": {
    "neon-cli": "^0.7.1",
    "typescript": "^4.2.3"
  },
  "scripts": {
    "install": "neon build --release",
    "build": "tsc && neon build --release"
  },
  "devDependencies": {
    "@types/node": "^14.14.32"
  }
}
